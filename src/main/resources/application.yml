spring:
  application:
    name: cardex-spring-cloud-api-gateway-service
  main:
    web-application-type: reactive
  cloud:
    gateway:
      routes:
        - id: stock-enquiry
          uri: http://localhost:8700/
          predicates:
            - Path=/product-enquiry/**


server:
  port: ${port:8900}



